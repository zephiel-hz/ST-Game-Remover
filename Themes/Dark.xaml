<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <LinearGradientBrush x:Key="BackgroundBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="#050505" Offset="0"/>
    <GradientStop Color="#101010" Offset="1"/>
  </LinearGradientBrush>
  <SolidColorBrush x:Key="ForegroundBrush" Color="#F4F2EA"/>
  <SolidColorBrush x:Key="MutedForegroundBrush" Color="#8C94A5"/>
  <SolidColorBrush x:Key="AccentBrush" Color="#F28C28"/>
  <SolidColorBrush x:Key="BorderBrush" Color="#2C3240"/>
  <SolidColorBrush x:Key="DangerButtonBrush" Color="#FF6B4A"/>
  <SolidColorBrush x:Key="PrimaryButtonBrush" Color="#1F242D"/>
  <SolidColorBrush x:Key="SuccessButtonBrush" Color="#4AA96C"/>
  <SolidColorBrush x:Key="WarningButtonBrush" Color="#C97C2D"/>
  <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="#1A1F29"/>
  <SolidColorBrush x:Key="ButtonBorderBrush" Color="#2A3140"/>
  <SolidColorBrush x:Key="ButtonIconBackground" Color="#161922"/>
  <SolidColorBrush x:Key="ButtonIconHoverBackground" Color="#242A35"/>
  <SolidColorBrush x:Key="ButtonHoverBorderBrush" Color="#3E4553"/>
  <SolidColorBrush x:Key="ButtonPressedBackground" Color="#151A22"/>
  <SolidColorBrush x:Key="PurpleButtonBrush" Color="#A86BDA"/>
  <SolidColorBrush x:Key="HoverBrush" Color="#212736"/>
  <SolidColorBrush x:Key="SelectedBrush" Color="#302114"/>
  <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#101010"/>
  <SolidColorBrush x:Key="CardBackgroundBrush" Color="#141414"/>
  <SolidColorBrush x:Key="InputBackgroundBrush" Color="#181818"/>
  <SolidColorBrush x:Key="InputForegroundBrush" Color="#F4F2EA"/>
  <SolidColorBrush x:Key="ComboBoxBackgroundBrush" Color="#181818"/>
  <SolidColorBrush x:Key="ComboBoxForegroundBrush" Color="#F4F2EA"/>
  <SolidColorBrush x:Key="ComboBoxItemForegroundBrush" Color="#F4F2EA"/>
  <SolidColorBrush x:Key="PlaceholderBrush" Color="#7B8390"/>
  <SolidColorBrush x:Key="ScrollBarBackgroundBrush" Color="#151821"/>
  <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#4A5362"/>
  <SolidColorBrush x:Key="ScrollBarThumbHoverBrush" Color="#F28C28"/>
  <SolidColorBrush x:Key="AlternatingRowBrush" Color="#1B212C"/>

  <Style TargetType="GridSplitter">
    <Setter Property="Background" Value="#1FFFFFFF" />
    <Setter Property="Width" Value="5" />
    <Setter Property="HorizontalAlignment" Value="Center" />
  </Style>

  <Style TargetType="ScrollBar">
    <Setter Property="Background" Value="{DynamicResource ScrollBarBackgroundBrush}"/>
    <Setter Property="Width" Value="10"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ScrollBar">
          <Grid>
            <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
            <Track x:Name="PART_Track" IsDirectionReversed="True" IsEnabled="{TemplateBinding IsMouseOver}">
              <Track.Thumb>
                <Thumb>
                  <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                      <Border Background="{DynamicResource ScrollBarThumbBrush}" CornerRadius="5" Margin="1"/>
                    </ControlTemplate>
                  </Thumb.Template>
                </Thumb>
              </Track.Thumb>
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ListViewItem">
    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
    <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
    <Setter Property="Margin" Value="0"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListViewItem">
          <Border Background="{TemplateBinding Background}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0" CornerRadius="0" Padding="{TemplateBinding Padding}">
            <ContentPresenter/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
      </Trigger>
      <DataTrigger Binding="{Binding IsSelected}" Value="True">
        <Setter Property="Background" Value="{DynamicResource SelectedBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
      </DataTrigger>
      <Trigger Property="ItemsControl.AlternationIndex" Value="0">
        <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
      </Trigger>
      <Trigger Property="ItemsControl.AlternationIndex" Value="1">
        <Setter Property="Background" Value="{DynamicResource AlternatingRowBrush}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style TargetType="ComboBoxItem">
    <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource ComboBoxItemForegroundBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
  </Style>

  <Style TargetType="GridViewColumnHeader">
    <Setter Property="Background" Value="{DynamicResource HeaderBackgroundBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="0,0,1,1"/>
    <Setter Property="Padding" Value="8,6"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="GridViewColumnHeader">
          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
      </Trigger>
      <Trigger Property="Role" Value="Padding">
        <Setter Property="Width" Value="0"/>
        <Setter Property="Visibility" Value="Collapsed"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style TargetType="CheckBox">
    <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <Grid>
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2" Width="16" Height="16">
              <Path x:Name="CheckMark" Data="M 0 5 L 5 10 L 12 0" Stroke="{DynamicResource ForegroundBrush}" StrokeThickness="2" Visibility="Collapsed"/>
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
              <Setter Property="Background" Value="{DynamicResource PrimaryButtonBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>